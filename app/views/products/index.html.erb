
<div id="myCarousel" class="carousel slide">
    <!-- 轮播（Carousel）指标 -->
    <ol class="carousel-indicators">
        <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
        <li data-target="#myCarousel" data-slide-to="1"></li>
        <li data-target="#myCarousel" data-slide-to="2"></li>
    </ol>
    <!-- 轮播（Carousel）项目 -->

    <div class="carousel-inner">

        <div class="item active">

            <img src="http://ww4.sinaimg.cn/large/006tNbRwgy1fga4uo6ztsj31f40gohdt.jpg" alt="First slide" style="width:160%">
            <div class="carousel-caption">每一个生命来到世间，都注定改变世界。</div>

        </div>
        <div class="item">
            <img src="http://ww1.sinaimg.cn/large/006tNbRwgy1fga5bni2evj31f40gox6p.jpg" alt="Second slide" style="width:110%">
            <div class="carousel-caption">真嫉妒你们能遇到这么牛的老师！</div>
        </div>
        <div class="item">
            <img src="http://ww3.sinaimg.cn/large/006tNbRwgy1fga54kbdfmj31fo0h6kjl.jpg" alt="Third slide" style="width:100%">
            <div class="carousel-caption">就是这么硬朗，彪悍！</div>
        </div>
    </div>
    <!-- 轮播（Carousel）导航 -->
    <a class="carousel-control left" href="#myCarousel"
        data-slide="prev">&lsaquo;
    </a>
    <a class="carousel-control right" href="#myCarousel"
        data-slide="next">&rsaquo;
    </a>
</div>

<div class="row">
  <% @products.each do |product| %>
    <div class="col-xs-6 col-md-3">
      <%= link_to product_path(product) do %>
        <% if product.image.present? %>
          <%= image_tag(product.image.thumb.url, class: "thumbnail") %>
        <% else %>
          <%= image_tag("http://placehold.it/200x200&text=No Pic", class: "thumbnail") %>
        <% end %>
      <% end %>
      <%= product.title %> ￥ <%= product.price %>

      <% if !product.users.include?(current_user) %>
         <%= link_to("",add_to_favorite_product_path(product),method: :post, class: "fa fa-heart") %>
       <% else %>
         <%= link_to("", quit_favorite_product_path(product),method: :post,  class: "fa fa-heart", style: "color:red;") %>
       <% end %>
    </div>
  <% end %>
</div>
